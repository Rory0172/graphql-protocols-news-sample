type User implements Postbox {
}

type Query implements Postbox, Posts {
  messages: [Message!]
  posts: [Post!]
  viewer: User
}

type Mutation implements PostboxMutatations {
  sendMessage(message: String): Message
}